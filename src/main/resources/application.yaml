spring.application.name: DemoApp
spring:
  jpa:
    hibernate:
      ddl-auto: validate

exchanges.apis:
  configs:
    - url: https://api.frankfurter.dev/v1/latest
      serviceId: frankfurtApi
    - url: someother
      serviceId: otherId
